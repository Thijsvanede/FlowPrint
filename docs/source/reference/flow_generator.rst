.. _FlowGenerator:

FlowGenerator
=============
The FlowGenerator class generates Flow objects from packets extracted by :ref:`Reader`.
To convert features from individual packets to Flows, we use the :py:meth:`flows.Flows.combine` method.

.. autoclass:: flow_generator.FlowGenerator
    :members:
